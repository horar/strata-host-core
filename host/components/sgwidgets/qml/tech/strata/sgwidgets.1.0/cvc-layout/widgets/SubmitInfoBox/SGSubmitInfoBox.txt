LayoutSGSubmitInfoBox { // start_%1
        id: sgSubmitInfoBox_%1
        layoutInfo.uuid: "%1"
        layoutInfo.columnsWide: 6
        layoutInfo.rowsTall: 2
        layoutInfo.xColumns: 0
        layoutInfo.yRows: 0

        // start_user_content
        placeholderText : "0-100"
        validator: IntValidator {    // Default: no input validator - you may assign your own configured DoubleValidator, IntValidator or RegExpValidator
            bottom: 0
            top: 100
        }
        onAccepted: {
            console.log("Accepted:",text,layoutInfo.uuid)
        }
        // end_user_content
    } // end_%1
