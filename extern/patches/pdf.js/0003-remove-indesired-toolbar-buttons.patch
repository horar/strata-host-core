diff --git a/web/viewer.css b/web/viewer.css
index 281e1f53..04f7f938 100644
--- a/web/viewer.css
+++ b/web/viewer.css
@@ -843,27 +843,48 @@ html[dir='rtl'] .toolbarButton.pageDown::before {
   content: url(images/toolbarButton-presentationMode.png);
 }
 
+.toolbarButton.presentationMode,
+.secondaryToolbarButton.presentationMode {
+  display: none !important;
+}
+
 .toolbarButton.print::before,
 .secondaryToolbarButton.print::before {
   content: url(images/toolbarButton-print.png);
 }
 
+.toolbarButton.print,
+.secondaryToolbarButton.print {
+  display: none !important;
+}
+
 .toolbarButton.openFile::before,
 .secondaryToolbarButton.openFile::before {
   content: url(images/toolbarButton-openFile.png);
 }
 
+.toolbarButton.openFile,
+.secondaryToolbarButton.openFile {
+  display: none !important;
+}
+
 .toolbarButton.download::before,
 .secondaryToolbarButton.download::before {
   content: url(images/toolbarButton-download.png);
 }
 
+.toolbarButton.download,
+.secondaryToolbarButton.download {
+  display: none !important;
+}
+
 .toolbarButton.bookmark,
 .secondaryToolbarButton.bookmark {
   box-sizing: border-box;
   outline: none;
   padding-top: 4px;
   text-decoration: none;
+  display: none !important;
 }
 .secondaryToolbarButton.bookmark {
   padding-top: 5px;
@@ -1000,6 +1021,10 @@ html[dir="rtl"] .secondaryToolbarButton > span {
   content: url(images/secondaryToolbarButton-documentProperties.png);
 }
 
+.secondaryToolbarButton.documentProperties {
+  display: none !important;
+}
+
 .verticalToolbarSeparator {
   display: block;
   padding: 8px 0;
@@ -1661,21 +1686,41 @@ html[dir='rtl'] #documentPropertiesOverlay .row > * {
     content: url(images/toolbarButton-presentationMode@2x.png);
   }
 
+  .toolbarButton.presentationMode,
+  .secondaryToolbarButton.presentationMode {
+    display: none !important;
+  }
+
   .toolbarButton.print::before,
   .secondaryToolbarButton.print::before {
     content: url(images/toolbarButton-print@2x.png);
   }
 
+  .toolbarButton.print,
+  .secondaryToolbarButton.print {
+    display: none !important;
+  }
+
   .toolbarButton.openFile::before,
   .secondaryToolbarButton.openFile::before {
     content: url(images/toolbarButton-openFile@2x.png);
   }
 
+  .toolbarButton.openFile,
+  .secondaryToolbarButton.openFile {
+    display: none !important;
+  }
+
   .toolbarButton.download::before,
   .secondaryToolbarButton.download::before {
     content: url(images/toolbarButton-download@2x.png);
   }
 
+  .toolbarButton.download,
+  .secondaryToolbarButton.download {
+    display: none !important;
+  }
+
   .toolbarButton.bookmark::before,
   .secondaryToolbarButton.bookmark::before {
     content: url(images/toolbarButton-bookmark@2x.png);
@@ -1752,6 +1797,10 @@ html[dir='rtl'] #documentPropertiesOverlay .row > * {
     content: url(images/secondaryToolbarButton-documentProperties@2x.png);
   }
 
+  .secondaryToolbarButton.documentProperties {
+    display: none !important;
+  }
+
   .outlineItemToggler::before {
     transform: scale(0.5);
     top: -1px;
