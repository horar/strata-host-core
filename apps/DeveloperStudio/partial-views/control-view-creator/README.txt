To run the editor, you need to install the monaco-editor npm package.

To do this, make sure that you have the control-view-creator directory as your current directory and run `npm install --save monaco-editor`.